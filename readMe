Take one of your processing exercises (or several) and change it into Object Orientated Programming. Uses classes to create objects, which enable you to have several different instances of the object. These instances should look different. Use properties/data and functions/methods to do this.
